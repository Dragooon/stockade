{
  "name": "agent-platform",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm -r --parallel start"
  }
}
